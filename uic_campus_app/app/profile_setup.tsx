import { Text, View } from "react-native";

export default function ProfileSetup() {
  return (
    <View
      style={{
        flex: 1,
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      <Text>This is the profile_setup.tsx
      </Text>
    </View>
  );
}
